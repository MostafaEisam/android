<?xml version="1.0" encoding="utf-8"?>
<manifest package="me.opklnm102.layout"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListAnimActivity">
        </activity>
        <activity android:name=".GridAnimActivity">
        </activity>
        <activity android:name=".ActivityAnimActivity">
        </activity>
        <activity android:name=".ViewFliperActivity">

        </activity>
        <activity android:name=".TextSwitcherActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>